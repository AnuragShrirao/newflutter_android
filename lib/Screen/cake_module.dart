//Todo cake model having different price according to weight

import 'package:flutter/material.dart';

class CakeModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
